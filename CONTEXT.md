# CC Port 资源管理

CC Port 资源管理上下文描述远端事实、本机观察与 AI 工具安装之间的边界。这里的术语用于桌面端、CLI、服务层和产品文档中的一致表达。

本上下文适用于 CC Port `0.5.0`，规范仓库地址为 <https://github.com/Ling-ye/cc-port>。

## Language

**远端资源仓库（Remote Resource Repository）**：
用户控制的跨设备事实源，保存可跨设备同步的资源内容、引用与元数据。
_Avoid_: 本地仓库、同步目录、中转目录

**仓库绑定（Repository Binding）**：
把用户提供的完整远端资源仓库根链接设为桌面端同步目标；只有远端读写权限验证通过后，绑定才成立。
_Avoid_: GitHub 登录、账号绑定、创建仓库

**受管远端镜像（Managed Remote Mirror）**：
CC Port 在本机隐藏维护、可从远端重建的派生缓存；它不是用户编辑资源或 AI 工具直接读取的工作区。
_Avoid_: 本地工作区、本地资源仓库、可编辑中枢

**远端快照（Remote Snapshot）**：
由远端资源仓库某个 commit 标识的只读资源视图，是清单比较和操作计划的远端依据。
_Avoid_: 当前工作区、远端目录

**本地实例（Local Instance）**：
存在于某个 AI 工具原生目标中的一份资源实例；同一逻辑资源可以有零个、一个或多个本地实例。
_Avoid_: 本地仓库副本、镜像副本

**刷新远端（Refresh Remote）**：
检查远端资源仓库并更新受管远端镜像与远端快照的读取操作，不安装或改写本地实例。
_Avoid_: 拉取工作区、同步全部、一键更新

**扫描本地（Scan Local）**：
在用户选择的全局与项目范围内只读发现本地实例的操作，不刷新或写入远端资源仓库。
_Avoid_: 环境同步、本地更新

**会话扫描范围（Session Scan Scope）**：
本次桌面应用会话最近一次本地扫描采用的全局与项目选择；应用重启后不保留。
_Avoid_: 永久扫描配置

**上传到仓库（Upload to Repository）**：
把用户选定的本地实例按已确认计划写入远端资源仓库的操作。
_Avoid_: 上传所选、同步到云端

**安装到工具（Install to Tools）**：
把用户选定的远端资源按已确认计划写入一个或多个 AI 工具原生目标的操作。
_Avoid_: 下载所选、链接镜像、同步到本地
